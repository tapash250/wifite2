name: wifite2_mobile_bridge
description: A Flutter mobile bridge for the Wifite2 wireless auditing tool.
publish_to: 'none'
version: 0.1.0+1
environment:
  sdk: ">=2.19.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.2.8+1
  websocket_client: ^2.0.0
  path_provider: ^2.0.15
  provider: ^6.0.5
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true